---
device_type: sensor
desc: A generic sensor that is taking measurements of some value
device_states:
  zero:
    sensor.generic:
      # Set entity attributes such as:
      # - native_value
      # - native_unit_of_measurement
      # - device_class
      native_value: 0
entities:
  sensor:
    generic:
      state_class: sensor.SensorStateClass.MEASUREMENT
