---
device_type: switch
desc: A generic switch.
device_states:
  "on":
    switch.switch: "on"
  "off":
    switch.switch: "off"
entities:
  switch:
    switch:
      device_class: switch.SwitchDeviceClass.SWITCH
