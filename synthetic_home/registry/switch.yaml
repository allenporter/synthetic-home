---
device_type: switch
desc: A generic switch.
device_states:
  "on":
    switch.switch: true
  "off":
    switch.switch: false
entities:
  switch:
    switch:
      device_class: switch.SwitchDeviceClass.SWITCH
